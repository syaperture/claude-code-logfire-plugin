[project]
name = "claude-code-logfire-plugin"
version = "0.5.0"
requires-python = ">=3.7"

[dependency-groups]
dev = ["ruff"]

[tool.ruff]
target-version = "py37"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["SIM105"]
